services:
  aplicativo_web:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: gerenciamento_frota_dev # nome do contêiner
    restart: unless-stopped
    ports:
      - "8989:8989"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    env_file:
      - .env.dev
    environment:
      - FLASK_ENV=development
    volumes:
      - .:/app
      - ./logs:/app/logs